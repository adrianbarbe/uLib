@model RemoteFinder.Models.FileStorage

<div class="container">
    <a href="@Url.Action("Index", "Home")">< Back</a>
    <div class="row">
        Are you sure you want to delete the item: @Model.Name?
    </div>
    <div class="row">
        @using (Html.BeginForm())
        {
            <div>
                <button type="submit" class="btn btn-default btn-lg">Delete</button>
            </div>
        }
    </div>
</div>